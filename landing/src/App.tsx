import { Landing } from './site/Landing'

/**
 * A fast, honest, conversion-focused landing: show the real product and its
 * value, get the visitor into the reader. (The earlier cinematic scroll-journey
 * — engine/, scenes/, portals — is retired; those files are no longer imported,
 * so none of that ships in the bundle.)
 */
export default function App() {
  return <Landing />
}

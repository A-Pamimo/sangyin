# Test/dev dependencies (not needed to run the server).
-r requirements.txt
pytest>=8.0
httpx2             # FastAPI/starlette TestClient transport (httpx is deprecated there)
reportlab>=4.0     # generate sample PDFs (with outlines) for parsing tests
